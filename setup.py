#!/usr/bin/env python

from distutils.core import setup

from setuptools import find_packages

setup(
    name='edgarl',
    version='0.1',
    description='EDGAR Local DB',
    packages=find_packages(),
)
